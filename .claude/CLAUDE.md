# Claude Code Instructions

Use the repository root `AGENTS.md` as the shared source of truth for Angular, TypeScript, accessibility, and MCP usage instructions.

This repo uses the `angular-cli` MCP server. For Angular-specific work, follow the Angular MCP routing rules in `AGENTS.md` and prefer the MCP server for current Angular behavior, official best practices, workspace structure, migrations, APIs, CLI behavior, and version-specific details.
