apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: app-postgres
  namespace: app
spec:
  instances: 1
  storage:
    size: 10Gi
  bootstrap:
    initdb:
      database: magnum_opus
      owner: magnum_opus
